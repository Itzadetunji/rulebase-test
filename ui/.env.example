# Optional; leave empty during `bun run dev` to use same-origin `/api`
# proxied through Vite to the API server (localhost:3000).
# Set this when serving the SPA from elsewhere, e.g.:
# VITE_API_BASE_URL=http://localhost:3000
VITE_API_BASE_URL=
